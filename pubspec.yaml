name: recreo
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0  # Asegúrate de usar una versión compatible con tu SDK
  firebase_auth: ^4.16.1
  cloud_firestore: ^4.6.1
  change_app_package_name: ^1.4.0
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  curved_navigation_bar: ^1.0.6
  shared_preferences: ^2.0.9
  image_picker: ^0.8.5+3
  firebase_storage: ^11.7.7
  google_sign_in: ^6.1.4




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/  # Asegúrate de que la ruta es correcta
    - assets/icons/
    - assets/fonts/
    
